# 設計課題メモ (restart-with-sourcegenerator 時点)

現状の設計を棚卸しし、今後の実装に向けて課題を整理したメモ。
対象は `restart-with-sourcegenerator` ブランチ (6d4fe1e 時点) のコード。

## 現状アーキテクチャの概要

「フィールドに `[Configurable]` を付けるだけで、設定ファイル/ランタイムから値を構成可能にする」ことを目指した
Roslyn Incremental Source Generator ベースの構成。

```
src/Configurables/                 ... Source Generator 本体 (netstandard2.0, C# 11)
src/ConfigurablesSample/           ... 動作確認用 Unity プロジェクト (Unity 6.1)
  Packages/com.github.cj-bc.configurables/
    Editor/                        ... ConfigProviderSelector 用 PropertyDrawer (スタブ)
    Samples~/ConfigSystem/Scripts/ ... ConfigSystem (MonoBehaviour 部分), JsonConfigProvider, ConfigProviderSelector
```

`ConfigurablesGenerator` が生成するもの:

1. `ConfigurableAttribute` — フィールド/プロパティ用マーカー属性
2. `IConfigProvider` — `ConfigSystem.Config GetConfig()` を持つインターフェース
3. 対象クラスごと — `partial class X` に `Config` 構造体 (readonly フィールド + コンストラクタ) と `Configure(Config)` を追加
4. 集約 `ConfigSystem` — 全対象クラスの `Config` をまとめた構造体、各対象への `[SerializeField]` 参照、
   `m_ConfigProvider.GetConfig()` の結果を各対象へ配る `Configure()`

想定フロー: Provider が設定を読み込み → `ConfigSystem.Configure()` → 各 `[Configurable]` フィールドへ反映。

---

## A. アーキテクチャ上の構造的課題(実装前に方針決定が必要)

### A-1. アセンブリ境界と集約 ConfigSystem の矛盾

Source Generator はコンパイル単位 (= asmdef 単位) で動くため、集約型 `ConfigSystem.g.cs` は
**同一アセンブリ内の `[Configurable]` しか収集できない**。

サンプルでも `Character` (Assembly-CSharp) と `ConfigSystem` の MonoBehaviour 部分
(Configurables.Runtime asmdef) が別アセンブリに分かれてコンパイル不能になり、
対症療法的な修正を重ねた経緯がある (3dedeb1, 17fea87)。

複数 asmdef を使う実プロジェクトでは現設計のままだと成立しない。検討すべき選択肢:

- アセンブリごとに部分 ConfigSystem を生成し、ランタイムのレジストリで束ねる
- 集約をやめ、対象クラス自身が ConfigSystem に登録しに行くモデルにする
- 「Configurable は単一アセンブリに置く」制約を明示して割り切る

**これが最重要の意思決定ポイント。A-2, C-11 とあわせて他のほぼ全ての実装がこの決定に依存する。**

### A-2. 生成型が public で全アセンブリに重複定義される

`ConfigurableAttribute` / `IConfigProvider` / `ConfigSystem` を Generator が
**参照側アセンブリすべてに public で生成**するため、複数アセンブリから Generator を参照すると
同名 public 型が重複し曖昧参照 (CS0436 相当) になる。

一般的な解法:

- マーカー属性などは `internal` で生成する (アセンブリごとに独立)
- 共有が必要な型 (`IConfigProvider`, `ConfigSystem` の核) はランタイム DLL / パッケージの Runtime/ に実体を置き、
  Generator は生成しない

### A-3. コア機能が Samples~ に置かれている

`ConfigSystem` (MonoBehaviour 部分)・`JsonConfigProvider`・`ConfigProviderSelector` が
`Samples~/ConfigSystem` 配下にあり、package.json でも "this is required" と説明されている。

UPM の Samples はユーザーが任意で import するオプション扱いであり、必須コードの置き場として不適切。
Runtime/ への移動が必要 (A-2 の解決策とも整合する)。

### A-4. Generator DLL の配布経路が未整備

ビルドした Analyzer DLL を UPM パッケージへ配置し RoslynAnalyzer ラベルを付ける仕組み・手順が存在しない。
現状はサンプルプロジェクトでどう Generator を参照しているかも追えない状態。
ビルドスクリプト (または CI) で `src/Configurables` をビルドして
パッケージ配下へコピーするパイプラインが必要。

### A-5. Provider 抽象が「全体 Config を同期で一括取得」しかできない

`IConfigProvider.GetConfig()` は `ConfigSystem.Config` (アプリ全体の設定) を一括で返す同期メソッドのみ。

- 部分的な設定供給 (あるクラスの設定だけ持つ Provider) が表現できない
- 複数 Provider の合成 (デフォルト値 ← ファイル ← コマンドライン引数のような重ね合わせ) ができない
- 非同期取得 (ネットワーク・Addressables 等) ができない (master 時代にあった UniTask は restart で廃止)
- 書き戻し (`Save`) の経路がなく、ランタイムで変更した設定を永続化できない

---

## B. Generator 実装のバグ・正しさの問題

### B-6. プロパティに `[Configurable]` を付けると Generator がクラッシュ

`ConfigurableAttribute` は `AttributeTargets.Field | AttributeTargets.Property` を許可しているが、
`Emit` / `CreateConfigSystem` は対象メンバーを `((IFieldSymbol)m)` へ無条件キャストしている。
プロパティに付けると InvalidCastException で Generator が死ぬ。
`IPropertySymbol` 対応を入れるか、当面 `AttributeTargets.Field` のみに絞るかを決める。

### B-7. namespace 内のクラスで生成コードが壊れる

`ConfigSystem.g.cs` は対象クラスを `t.ClassSymbol.Name` (単純名) で参照する。

- namespace 内のクラスは `Configurables` namespace の生成コードから解決できずコンパイルエラー
- 異なる namespace の同名クラスはフィールド名・型参照が衝突

`ToDisplayString(FullyQualifiedFormat)` ベースの参照と、フィールド名の一意化 (namespace を含めた変換) が必要。
フィールド型の出力 (`((IFieldSymbol)m).Type` の暗黙 ToString) も完全修飾でないため同種の事故が起こり得る。

### B-8. ISymbol をパイプラインに保持している (Incremental Generator のアンチパターン)

中間モデル `Target` record が `INamedTypeSymbol` / `IEnumerable<ISymbol>` を保持している。

- ISymbol は Compilation を芋づる式に保持するため、IDE 上でメモリリークの原因になる
- record の等価比較も `IEnumerable` メンバーで実質壊れており、インクリメンタルキャッシュが一切効かない

属性収集の段階で「クラス名・namespace・メンバー名・型名 (すべて string)」の値ベース record へ変換するのが定石。

### B-9. 未完成のデッドコード

`Initialize` 末尾の `configProviders` パイプラインは構築だけして `RegisterSourceOutput` されていない
(最後の `.Select` がコメントアウト)。`nameOnlyDisplayFormat` も未使用。
また現状の実装は Provider 検出として不完全:

- `symbol.Interfaces` (直接実装のみ) を見ており、基底クラス経由の実装を拾えない (`AllInterfaces` が必要)
- インターフェース名の文字列一致のみで、別 namespace の同名インターフェースを誤検出する

ConfigProviderSelector 向け機能 (C-15) の実装時に作り直すこと。

### B-10. 生成コードの品質

- `ConfigSystem.g.cs` / `IConfigProvider.g.cs` に `// <auto-generated/>` ヘッダーがない
  (per-class 生成のみ付いている)。アナライザ抑制のため全生成ファイルに付けるべき
- generic クラス対応はファイル名の `<` `>` 置換のみで、生成コード本体は非対応
- インデントが崩れており、デバッグ時に生成コードが読みにくい

---

## C. ランタイム/サンプル実装の問題

### C-11. 生成される Config 構造体が JsonUtility で読めない

生成される `Config` は `public readonly` フィールド + コンストラクタのみだが、

- `JsonUtility` は readonly フィールドに書き込めない
- `FromJson<ConfigSystem.Config?>` の `Nullable<T>` も JsonUtility 非対応

のため、**`JsonConfigProvider` は設計レベルで機能しない**。シリアライズ戦略の決定が必要:

- readonly をやめて mutable な構造体にする
- 生成側でデシリアライズ用の中間 DTO も生成する
- JsonUtility をやめて別シリアライザ (System.Text.Json 等) を採用する

immutability を保ちたいなら中間 DTO 案が有力。

### C-12. Provider 設定がコードでハードコード上書きされる

`Samples~/ConfigSystem/Scripts/ConfigSystem.cs` の `Start()` が
`m_ConfigProvider = new JsonConfigProvider()` を直書きしており、
`[SerializeReference]` でインスペクタから設定した Provider を無視する。
さらに `JsonConfigProvider.m_Path` が未設定のままなので `File.ReadAllText(null)` で即例外になる。
Provider の選択・構成はシリアライズ + ConfigProviderSelector (C-15) に寄せるべき。

### C-13. エラーハンドリングが存在しない

- `JsonConfigProvider` は失敗時に `throw new System.Exception()` (情報ゼロ)
- `ConfigSystem.Configure()` は `m_ConfigProvider` / 各ターゲット参照の null チェックなし
- ファイル I/O (存在しない・権限なし・パース失敗) の例外処理なし

「設定が読めないときにゲームをどうするか」(デフォルト値で続行 / 明示エラー) の方針を決め、
専用例外型 or Result 型 + ログで統一する。

### C-14. ConfigSystem.Configure() を呼ぶ仕組みがない

生成された `Configure()` はどこからも呼ばれない。サンプルの `Character.Start()` は
`Configure(new(10, 1.0f))` とハードコード値で自己構成しており、
**Provider → ConfigSystem → 各対象、というライブラリ本来のフローが一度も通っていない**。
ConfigSystem の MonoBehaviour 部分のライフサイクル (Awake/Start のどこで読むか、
対象より先に初期化される保証、再読み込みの口) を設計する必要がある。

### C-15. ConfigProviderSelector / Editor 拡張がスタブ

- `ConfigProviderSelectorEditor.CreatePropertyGUI` は空の VisualElement を返すだけ
- `ConfigProviderSelector.ConfigProvider` は `[SerializeReference]` なしの interface 型フィールドで、
  そもそも Unity がシリアライズできない
- TypeCache で IConfigProvider 実装を列挙して選択させる構想はコメントの参照 URL のみ

機能させるには `[SerializeReference]` + 実装型ドロップダウンの PropertyDrawer 実装が必要。

---

## D. プロジェクト面の課題

### D-16. テストが皆無

Generator は Unity なしでテストできる部分が大きい
(Microsoft.CodeAnalysis.CSharp.SourceGenerators.Testing や Verify によるスナップショットテスト)。
B 群のバグはすべてテストで再現・固定できるため、Generator のテストプロジェクト追加を優先したい。
Unity 側 (Provider の I/O、ConfigSystem のライフサイクル) は Unity Test Framework で別途。

### D-17. ドキュメントがない

README がなく、使い方・前提 (asmdef 構成の制約、Samples の import が必須なこと)・拡張方法が
コードを読まないと分からない。少なくとも README に Quick Start と既知の制約を書く。

### D-18. バージョン表記の不整合

- package.json は `"unity": "2022.3"`、サンプルプロジェクトは Unity 6.1 (6d4fe1e)
- Generator は LangVersion 11 / Microsoft.CodeAnalysis.CSharp 4.3.0 固定。
  サポート対象の Unity バージョンが同梱する Roslyn とコンパイラバージョンの互換確認が必要

サポート対象 Unity の最低バージョンを決めて表記を揃える。

---

## 今後の実装ロードマップ案

### Phase 1: 方針決定(他のすべてがここに依存)

| 決めること | 関連課題 |
|---|---|
| アセンブリ境界戦略 (集約 ConfigSystem をどう成立させるか) | A-1 |
| 生成型の可視性・配置 (internal 生成 vs ランタイム DLL) | A-2, A-3 |
| Config のシリアライズ戦略 (readonly 維持 + DTO か、mutable 化か) | C-11 |

### Phase 2: 基盤修復

- Generator バグ修正: プロパティ対応 or Field 限定化 (B-6)、完全修飾名対応 (B-7)、
  値ベース中間モデル化 (B-8)、生成コード品質 (B-10)
- コアコードの Samples~ → Runtime/ 移動 (A-3)
- Analyzer DLL のビルド・パッケージ配置パイプライン (A-4)

### Phase 3: 機能の完成

- Provider のシリアライズ構成と選択 UI (C-12, C-15)、デッドコード作り直し (B-9)
- ConfigSystem のライフサイクル設計と Configure 呼び出しフロー (C-14)
- エラーハンドリング方針の実装 (C-13)
- Provider 抽象の再設計: 部分供給・合成・非同期・Save (A-5)

### Phase 4: 品質

- Generator スナップショットテスト + Unity テスト (D-16)
- README / 既知の制約のドキュメント化 (D-17)
- Unity / Roslyn バージョン整合 (D-18)
